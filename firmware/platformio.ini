; =============================================================================
; Timer Digital Relay v4.0 — PlatformIO configuration
; =============================================================================
; audit-fixes:
;   - Added [env:production] as default env with -DPRODUCTION_BUILD flag.
;     Production builds fail-closed on insecure defaults (TLS / auth / CORS / OTA).
;   - Use [env:development] for dev/testing without production guards.
;   - Added PubSubClient to lib_deps (was missing — build would fail).
;   - Pinned ArduinoJson to v6 (v7 removed DynamicJsonDocument/StaticJsonDocument).
; =============================================================================
[platformio]
default_envs = production
; audit-fixes: override src_dir to firmware/ root (default is firmware/src/).
;   The firmware uses a flat layout (all .cpp/.h/.ino at root) for Arduino IDE
;   compatibility. PlatformIO's default expects src/, so we override here.
src_dir = .

; ---------- Common flags shared by all envs ----------
[common]
build_flags =
  -std=gnu++17
  -DCORE_DEBUG_LEVEL=3
  -DBOARD_HAS_PSRAM=0
  -DARDUINO_USB_MODE=1
  -DARDUINO_USB_CDC_ON_BOOT=0
  ; AUD-FW-OTA-001 FIX (Opsi B, 2026-08-20): Ed25519 verification now uses
  ;   self-contained ed25519-donna library (firmware/ed25519-donna.c/.h).
  ;   No longer depends on PSA Crypto or MBEDTLS_ED25519_SUPPORTED flag.
  ;   Both MQTT OTA and REST OTA can now verify Ed25519 signatures.
  ;   The library uses mbedtls/sha512.h (already in framework, C++ compatible)
  ;   for SHA-512 internally. No framework rebuild required.
lib_deps =
  adafruit/RTClib@^2.1.4
  bblanchon/ArduinoJson@^6.19.0
  knolleary/PubSubClient@^2.8
  ; LittleFS is bundled with the ESP32 Arduino core (no separate lib_dep).
  ; mbedtls is bundled with esp32 platform — no separate dep needed
  ; Preferences is part of ESP32 core

; ---------- DEVELOPMENT env ----------
[env:development]
platform = espressif32@^6.5.0
board = esp32dev
board_build.mcu = esp32
board_build.f_cpu = 240000000L
framework = arduino
board_build.filesystem = littlefs
board_build.partitions = partitions_ota_1mb5.csv
build_flags =
  ${common.build_flags}
  -DDEVELOPMENT_BUILD
  -DBATTERY_MONITORING_ENABLED
lib_deps = ${common.lib_deps}
monitor_speed = 115200
monitor_filters = esp32_exception_decoder, time
upload_speed = 921600

; ---------- STAGING env ----------
[env:staging]
platform = espressif32@^6.5.0
board = esp32dev
board_build.mcu = esp32
board_build.f_cpu = 240000000L
framework = arduino
board_build.filesystem = littlefs
board_build.partitions = partitions_ota_1mb5.csv
build_flags =
  ${common.build_flags}
  -DSTAGING_BUILD
  -DBATTERY_MONITORING_ENABLED
lib_deps = ${common.lib_deps}
monitor_speed = 115200
monitor_filters = esp32_exception_decoder, time
upload_speed = 921600

; ---------- PRODUCTION env (DEFAULT) ----------
[env:production]
platform = espressif32@^6.5.0
board = esp32dev
board_build.mcu = esp32
board_build.f_cpu = 240000000L
framework = arduino
board_build.filesystem = littlefs
board_build.partitions = partitions_ota_1mb5.csv
build_flags =
  ${common.build_flags}
  -DPRODUCTION_BUILD
  -DBATTERY_MONITORING_ENABLED
lib_deps = ${common.lib_deps}
monitor_speed = 115200
monitor_filters = esp32_exception_decoder, time
upload_speed = 921600

; ---------- Backward-compat alias ----------
[env:esp32dev]
platform = espressif32@^6.5.0
board = esp32dev
board_build.mcu = esp32
board_build.f_cpu = 240000000L
framework = arduino
board_build.filesystem = littlefs
board_build.partitions = partitions_ota_1mb5.csv
build_flags =
  ${common.build_flags}
  -DDEVELOPMENT_BUILD
  -DBATTERY_MONITORING_ENABLED
lib_deps = ${common.lib_deps}
monitor_speed = 115200
monitor_filters = esp32_exception_decoder, time
upload_speed = 921600
